# TODO - Responsive Bootstrap + Fix link paths

## Plan (implementation steps)

1. Update `style/style.css` to make the layout responsive (nav, footer columns, product grid breakpoints).
2. Update `index.html` only (home page at project root) to use correct link/image paths and add responsive Bootstrap navbar/footer structure.
3. Ensure `index.html` references the correct stylesheet path (`style/style.css`) and images path (`images/...`).
4. Keep existing design classes, but add Bootstrap utility classes and a small set of CSS media queries to improve mobile behavior.
5. Quick manual check: open `index.html` and verify images/links work; confirm grid collapses properly on smaller screens.
6. Done: responsiveness CSS + `index.html` path fixes.
